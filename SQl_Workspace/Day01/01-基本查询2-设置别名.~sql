----查询所有雇员的所有信息,并给表做别名
select e.empno,
       e.ename,
       e.job,
       e.mgr,
       e.hiredate,
       e.sal,
       e.comm,
       e.deptno
from emp e;

--给列起别名并且查出年薪
select ename,sal,sal*12 "年薪"
from emp;
