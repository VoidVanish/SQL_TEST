----创建学生表-- 创建一个学生信息表（学号, 姓名, 性别，年龄，生日，手机号，家庭住址）
create table t_student (
       id    number(),
       name  varchar(40),
       sex   char(4),
       age   number(3),
       birthday        date, 
       phone_nub       char(11)

);

----复制表
create table t_emp
as
select * from emp;
